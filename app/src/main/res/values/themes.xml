<resources>
    <!-- Only used briefly before Compose takes over (window background, status bar). -->
    <style name="Theme.DigitalSpeedometer" parent="android:Theme.DeviceDefault.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowBackground">@color/ic_launcher_background</item>
    </style>
</resources>
